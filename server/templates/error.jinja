{% extends "layout.jinja" %}
{% block body %}
    <div class="flex flex-col gap-2 text-center">
        <h1 class="tracking-wider font-light">{{ code }}</h1>
        <p class="text-2xl font-medium tracking-[0.3rem] uppercase">{{ message }}</p>
        <a class="mt-8 underline decoration-dotted hover:decoration-solid"
           href="/">Back to Home</a>
    </div>
{% endblock %}
